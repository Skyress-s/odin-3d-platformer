package Spatial

Sphere_Trace :: distinct struct {
	ray: Ray,
	radius: f32,
}

sphere_trace_triangle_intersect :: proc() {
}

calculate_hashes_by_sphere_trace :: proc(sphere_trace: Sphere_Trace) -> (cells: map[Hash_Key]bool) {

	return cells
}
